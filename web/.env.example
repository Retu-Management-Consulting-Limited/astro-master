# Molly — environment template. Copy to .env.local and fill in.
# All AI/paid integrations are currently STUBBED; nothing below is required to
# run the app in dev. Add keys only when wiring the corresponding TODO().

# TODO(key): Claude readings (first-read, theme deep-read, chat).
# Use ONLY from a server route / server action — never expose in the client bundle.
ANTHROPIC_API_KEY=

# TODO(geo): real geocoding + historical timezone (replaces the built-in city table).
# GEOCODING_API_KEY=

# TODO(push): Web Push (VAPID) for daily / wealth / synastry reminders.
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
